class Category {
  constructor(id, title, imageUrl, galleries) {
    this.id = id;
    this.title = title;
    this.imageUrl = imageUrl;
    this.galleries = galleries;
  }
}

export default Category;
